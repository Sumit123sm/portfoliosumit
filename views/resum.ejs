<section id="projects" class="container py-5">
    <h2 class="text-center mb-4">Projects</h2>
    <div class="row g-4">
        <% projects.forEach(project => { %> <!-- ✅ Use `projects` correctly -->
            <div class="col-md-4">
                <div class="card shadow-lg h-100 border-0">
                    <img src="<%= project.image_url.replace('github.com', 'raw.githubusercontent.com').replace('/blob/', '/') %>" 
 class="card-img-top project-img" 
 alt="<%= project.title %>">

                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title"><b><%= project.title %></b></h5>
                        <p class="card-text flex-grow-1"><%= project.description %></p>
                        <a href="<%= project.project_link %>" class="btn btn-primary mt-auto" target="_blank" >View Project</a>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</section>
